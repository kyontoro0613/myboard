Mypage
<%= link_to('Logout', destroy_user_session_path, method: :delete) %>
<%= current_user.name %>
<p>
  <% if current_user.avatar.attached? %>
    <%= image_tag url_for(current_user.avatar), width: "200px" %>
  <% end %>
</p>
